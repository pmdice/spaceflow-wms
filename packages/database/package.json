{
  "name": "@spaceflow/database",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "index.ts",
  "scripts": {
    "build": "tsup index.ts --format cjs --clean --shims",
    "dev": "tsup index.ts --format cjs --watch",
    "db:push": "npx prisma db push",
    "db:generate": "npx prisma generate"
  },
  "dependencies": {
    "@prisma/adapter-pg": "6.19.3",
    "@spaceflow/config-env": "workspace:*",
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "@types/pg": "^8.18.0",
    "dotenv": "^16.4.7",
    "prisma": "6.19.3",
    "tsup": "^8.0.2",
    "typescript": "latest"
  }
}
